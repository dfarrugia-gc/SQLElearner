@model IEnumerable<Elearner.Models.QuestionAnswer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuestionAnswerText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionAnswerText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QuestionAnswerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.QuestionAnswerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QuestionAnswerId })
        </td>
    </tr>
}

</table>
