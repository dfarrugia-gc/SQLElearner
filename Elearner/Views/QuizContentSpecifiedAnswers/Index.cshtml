@model IEnumerable<Elearner.Models.QuizContentSpecifiedAnswer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuizContent.QuizContentId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SpecifiedAnswer.AnswerText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuizContentSpecifiedAnswerId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuizContent.QuizContentId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SpecifiedAnswer.AnswerText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuizContentSpecifiedAnswerId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
