@model IEnumerable<SQLElearner.Models.TopicType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TopicTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TopicTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TopicTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TopicTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TopicTypeId })
        </td>
    </tr>
}

</table>
